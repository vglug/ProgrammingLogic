#include<stdio.h>
void main()
{
	int j,i,n,a=-1,b=1,c=0;
	int d[20];
	scanf("%d",&n);
	i=0;
	while(i<n)
	{
		c=a+b;
		d[i]=c;
		a=b;
		b=c;
		i++;
	}
	for(j=i-1;j>=0;j--)
	{
		printf("%d ",d[j]);
	}
}
